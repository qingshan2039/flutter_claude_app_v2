---
task_id: T20.4
task_name: CONVENTIONS.md
module_id: M20
module_name: 文档交付
priority: P0
status: completed
verified_at: 2026-05-24
spec_source: flutter_template_v3.md
spec_lines: "814-818"
flutter_version: 3.41.9
dart_version: 3.11.5
deliverables:
  - docs/CONVENTIONS.md
tags: [docs, conventions, naming, code-style, very-good-analysis, conventional-commits, M20, T20.4]
---

# T20.4 CONVENTIONS.md — 验证报告

> 任务 T20.4 / 模块 M20 文档交付 / 优先级 P0 / 状态 ✅ completed / 验证 2026-05-24

## TL;DR

T20.4 新建 `docs/CONVENTIONS.md`：命名规范（通用 + 分层角色后缀表）、代码风格（dart format + very_good_analysis，逐条列出 14 条已关闭规则及原因，并警示 `dart fix` 风险）、Git 提交规范（Conventional Commits 正则 + lefthook 两钩子 + 分支/PR）。所有规则与仓库 `analysis_options.yaml`、`lefthook.yml`、`scripts/check_commit_msg.sh` 实际配置一一对齐。

## 1. 任务目标（来自 spec）

T20.4（spec 814-818）：需含**命名规范、代码风格、Git 提交规范**，交付物 `docs/CONVENTIONS.md`。

## 2. 实际配置

| spec 要求 | 实现 |
|---|---|
| 命名规范 | 通用命名表 + 分层角色（Entity/Model/Repository/Impl/DataSource/UseCase/Provider/Page）后缀表 |
| 代码风格 | dart format（80 列）+ very_good_analysis；14 条关闭规则原因表；`dart fix` 风险警示 |
| Git 提交规范 | Conventional Commits（type 清单 + 示例）+ lefthook（pre-commit/commit-msg）+ 分支/PR + codegen 须入库 |

## 3. 快速上手（Quick Start）

阅读 [docs/CONVENTIONS.md](../CONVENTIONS.md)；提交前 `lefthook install` 后规则自动执行。

## 4. 验收命令与实际输出

```
$ # 关闭规则数与 analysis_options.yaml 一致（14 条；含名称带数字的 lines_longer_than_80_chars）
$ grep -cE "^\s+[a-z0-9_]+: false" analysis_options.yaml
14

$ flutter analyze
No issues found! (ran in 3.6s)
```

提交 type 清单（feat/fix/docs/style/refactor/perf/test/build/ci/chore/revert）与 `scripts/check_commit_msg.sh` 的正则完全一致。

## 5. 与 spec 的偏差与决策

T20.4 决策：把「为什么关闭某 lint 规则」逐条写明（含 `avoid_redundant_argument_values` / `unnecessary_lambdas` 曾改坏代码的真实教训），并显式警示「不要盲跑 `dart fix --apply`」——这是本模板踩过的坑，写进规范避免复发。

## 6. 关键文件清单

| 文件 | 说明 |
|---|---|
| `docs/CONVENTIONS.md` | 约定规范（新建） |

## 7. 下一步任务

T20.5 docs/EXTEND_GUIDE.md（补全新增 feature / 权限 / 环境章节）。

## 8. 环境快照

Flutter 3.41.9 · Dart 3.11.5 · very_good_analysis 10.2.0 · lefthook · macOS。
