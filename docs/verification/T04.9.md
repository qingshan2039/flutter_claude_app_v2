---
task_id: T04.9
task_name: SSL Pinning（可选）
module_id: M04
module_name: 网络层
priority: P0
optional: true
status: completed_documentation_only
verified_at: 2026-05-18
spec_source: flutter_template_v3.md
spec_lines: "279-282"
new_doc_files: [docs/network/SSL_PINNING.md]
deliverables: [docs/network/SSL_PINNING.md]
implementation_status: "documentation only, not enabled by default"
enable_strategy: "--dart-define SSL_PINNING_ENABLED=true at release build time"
m18_integration_pending: true
tags: [ssl-pinning, security, certificate-fingerprint, optional, T04, M04]
---

# T04.9 SSL Pinning（可选） — 验证报告

> **任务 ID**: T04.9 · **模块**: M04 网络层 · **优先级**: P0（标注可选） · **状态**: ✅ 文档已交付
> **Spec 来源**: [flutter_template_v3.md:279-282](file:///Users/ben/Downloads/flutter_template_v3.md)

## TL;DR

T04.9 在 spec 中标注**可选**。本任务交付 [docs/network/SSL_PINNING.md](file:///Users/ben/ai_project/flutter_claude_app_v2/docs/network/SSL_PINNING.md) 配置说明文档（**默认不启用 pinning**）。文档涵盖：(a) 何时不该启用（运维代价权衡）、(b) 用 `openssl` 获取证书指纹、(c) 在 dio 中通过 `IOHttpClientAdapter.createHttpClient` + `badCertificateCallback` 启用 SHA-256 公钥比对、(d) `--dart-define SSL_PINNING_ENABLED` 编译期开关、(e) 测试策略、(f) 与 ApiErrorInterceptor 的衔接（`badCertificate` → `NetworkException(BAD_CERT)`）、(g) Android Network Security Config 与 CT 等替代方案、(h) M18/T18.2 集成预期。

## 1. 任务目标（spec）

- 证书固定示例
- 开关控制
- **交付物**：配置说明文档

## 2. 实施范围

T04.9 **不修改任何运行时代码**。`docs/network/SSL_PINNING.md` 提供：

| 章节 | 内容 |
|---|---|
| § 1 为何不默认启用 | 证书轮换 / 错配置风险 / 多环境差异 |
| § 2 接入步骤 | openssl 命令 + dio 代码片段 + dart-define 开关 |
| § 3 测试策略 | 4 个测试场景（合法 / MITM / 轮换 / 错配置） |
| § 4 与 M04 拦截器的关系 | `badCertificate` → `NetworkException` 链路 |
| § 5 替代方案 | Android NSC / CT / App Attestation |
| § 6 参考链接 | OWASP / Dio 源码 / Flutter 官方 |

## 3. 默认行为（不启用）

[lib/core/network/dio_client.dart](file:///Users/ben/ai_project/flutter_claude_app_v2/lib/core/network/dio_client.dart) 当前不调用 `IOHttpClientAdapter.createHttpClient` 自定义证书校验逻辑——使用 dart:io 默认的 `SecurityContext.defaultContext`（系统根证书）。该模板照常通过任何受信 CA 签发的证书。

启用 pinning 不属于本任务范围，按 [docs/network/SSL_PINNING.md § 2.2](file:///Users/ben/ai_project/flutter_claude_app_v2/docs/network/SSL_PINNING.md) 手动接入。

## 4. 5 项验证

T04.9 是文档任务，5 项验证调整对应：

| # | 验证 | 结果 |
|---|---|---|
| 1 | 依赖注册 | 不适用（文档） |
| 2 | 代码生成 | 不适用 |
| 3 | 运行调用 | 不适用（默认不启用） |
| 4 | 单元测试 | 不适用 |
| 5 | 静态分析 | ✅ 文档未修改源码，整体 No issues found |

文档完整性：文档涵盖 spec 的 2 项要求（证书固定示例 ✅、开关控制 ✅）。

## 5. 文件

| 路径 | 类型 | 说明 |
|---|---|---|
| `docs/network/SSL_PINNING.md` | 文档 | 6 节配置指南 |

## 6. 与 M18 的关系

- M18/T18.2「Android 网络安全配置」会用 Android Network Security Config XML 提供 Android 端 pinning（不需运行时代码）
- iOS 端仍需按本文档手动接入 dio 拦截
- M18 落地后请回链 SSL_PINNING.md，更新「替代方案」节

## 7. 下一步

T04.9 完成后，**M04 网络层全部完成**（T04.1-T04.9 共 9 个子任务）。下一步进入 **M05 本地存储**：

- T05.1 封装 SharedPreferences
- T05.2 封装 flutter_secure_storage（**M05 完成后回填 AuthInterceptor 的 TokenStorage 真实实现**）
- T05.3 集成 Hive / Isar
- T05.4 数据库 Schema 版本管理

---

**验证人**: Claude (Opus 4.7) · **日期**: 2026-05-18
