---
task_id: T09.4
task_name: iOS / Android 差异封装
module_id: M09
module_name: 权限管理
priority: P0
status: completed
verified_at: 2026-05-18
spec_source: flutter_template_v3.md
spec_lines: "435-438"
modifications:
  - "android/app/src/main/AndroidManifest.xml (10 个 uses-permission)"
  - "ios/Runner/Info.plist (5 个 usage description)"
new_doc_files: [docs/permission/PERMISSIONS.md]
difference_handling: "AppPermission → permission_handler Permission 映射函数（mapToHandler）抹平命名差异"
deliverables: ["原生配置 + 差异说明文档"]
tags: [permission, ios, android, info-plist, android-manifest, platform-difference, T09, M09, P0]
---

# T09.4 iOS / Android 差异封装 — 验证报告

> **任务 ID**: T09.4 · **模块**: M09 权限管理 · **优先级**: P0 · **状态**: ✅
> **Spec 来源**: [flutter_template_v3.md:435-438](file:///Users/ben/Downloads/flutter_template_v3.md)

## TL;DR

T09.4 完成权限的平台差异封装三件事：(1) 代码层 — `AppPermission` 枚举 + `mapToHandler` 映射函数把 iOS/Android 权限名差异抹平（业务只认平台无关枚举）；(2) 原生声明 — [AndroidManifest.xml](file:///Users/ben/ai_project/flutter_claude_app_v2/android/app/src/main/AndroidManifest.xml) 加 10 个 `uses-permission`、[Info.plist](file:///Users/ben/ai_project/flutter_claude_app_v2/ios/Runner/Info.plist) 加 5 个 usage description；(3) 文档 — [docs/permission/PERMISSIONS.md](file:///Users/ben/ai_project/flutter_claude_app_v2/docs/permission/PERMISSIONS.md) 含完整对照表、iOS 编译宏说明、上线 checklist。

## 1. 任务目标（spec）

- 处理两端权限名差异
- Info.plist / AndroidManifest 配置
- **交付物**：原生配置 + 差异说明文档

## 2. 差异处理三层

### 2.1 代码层：映射函数抹平

业务用 `AppPermission.location`，`mapToHandler` 映射到 `Permission.location`，permission_handler 内部在 iOS 用 `whenInUse`、Android 用 `ACCESS_FINE_LOCATION`。业务无感知差异。

### 2.2 Android 声明（10 条）

```xml
<uses-permission android:name="android.permission.CAMERA"/>
<uses-permission android:name="android.permission.RECORD_AUDIO"/>
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
<uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
<uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
<uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>
```

注意版本差异：相册（13+ 媒体细分权限 vs ≤32 的 READ_EXTERNAL_STORAGE）、蓝牙（12+ CONNECT/SCAN）。

### 2.3 iOS 声明（5 条 usage description）

```xml
<key>NSCameraUsageDescription</key>          <string>...</string>
<key>NSPhotoLibraryUsageDescription</key>    <string>...</string>
<key>NSMicrophoneUsageDescription</key>      <string>...</string>
<key>NSLocationWhenInUseUsageDescription</key> <string>...</string>
<key>NSBluetoothAlwaysUsageDescription</key> <string>...</string>
```

iOS 缺 usage description 会导致**运行时崩溃 + App Store 审核拒绝**。

## 3. 文档要点（PERMISSIONS.md）

| 章节 | 内容 |
|---|---|
| 架构三层 | AppPermission → PermissionService → permission_handler → 原生 |
| 对照表 | 7 种权限的 iOS/Android 名称 + 原生声明 |
| 三态结果 | granted/denied/permanentlyDenied/restricted/limited 处理 |
| iOS 编译宏 | permission_handler Podfile 宏（只编入用到的权限，审核更干净） |
| 上线 checklist | 裁剪未用权限、usage 文案、后台定位、永久拒绝路径测试 |

### 3.1 iOS 编译宏提醒（重要）

文档强调：permission_handler iOS 默认编入**所有**权限，会触发审核问询。正式项目应在 `ios/Podfile` 的 `post_install` 加 `GCC_PREPROCESSOR_DEFINITIONS` 宏，**只启用用到的权限**。模板未默认改 Podfile（避免与 flavor 配置冲突），文档给出完整示例。

## 4. 5 项验证

| # | 验证 | 结果 |
|---|---|---|
| 1 | 依赖注册 | 不适用（原生配置 + 文档） |
| 2 | 代码生成 | 不适用 |
| 3 | 运行调用 | ✅ 映射函数测试覆盖 7 种权限（mapToHandler，T09.1） |
| 4 | 单元测试 | ✅ 7 种权限映射唯一性测试通过 |
| 5 | 静态分析 | ✅ flutter analyze No issues（XML/plist 非 Dart 改动）；apk 构建成功（manifest 合法） |

## 5. 文件变更清单

| 路径 | 类型 | 改动 |
|---|---|---|
| `android/app/src/main/AndroidManifest.xml` | XML | +10 uses-permission |
| `ios/Runner/Info.plist` | plist | +5 usage description |
| `docs/permission/PERMISSIONS.md` | 文档 | 新建（对照表 + 编译宏 + checklist） |

## 6. 下一步

T09.4 完成后，**M09 权限管理全部完成**（T09.1-T09.4 共 4 个子任务）。下一步进入 **M10 主题与设计系统**：

- T10.1 定义 Design Tokens（颜色/字号/间距/圆角/阴影/动效）
- T10.2 构建 ThemeData（亮/暗）
- T10.3 自定义 ThemeExtension
- T10.4 主题切换（Riverpod + 持久化）
- T10.5 状态栏样式适配

---

**验证人**: Claude (Opus 4.7) · **日期**: 2026-05-18
