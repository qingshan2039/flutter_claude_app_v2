---
task_id: T20.3
task_name: ARCHITECTURE.md
module_id: M20
module_name: 文档交付
priority: P0
status: completed
verified_at: 2026-05-24
spec_source: flutter_template_v3.md
spec_lines: "808-812"
flutter_version: 3.41.9
dart_version: 3.11.5
deliverables:
  - docs/ARCHITECTURE.md
tags: [docs, architecture, clean-architecture, feature-first, data-flow, mermaid, M20, T20.3]
---

# T20.3 ARCHITECTURE.md — 验证报告

> 任务 T20.3 / 模块 M20 文档交付 / 优先级 P0 / 状态 ✅ completed / 验证 2026-05-24

## TL;DR

T20.3 新建 `docs/ARCHITECTURE.md`：用 3 张 Mermaid 图覆盖 spec 要求的**架构图 / 数据流图**——分层依赖图、登录序列图（数据流）、bootstrap 启动流程图；并以表格给出**分层职责**（presentation/domain/data/core/shared 的职责、允许依赖、禁止项）、依赖规则、目录映射、关键横切设计。

## 1. 任务目标（来自 spec）

T20.3（spec 808-812）：需含**架构图、分层职责、数据流图**，交付物 `docs/ARCHITECTURE.md`。

## 2. 实际配置

| spec 要求 | 实现 |
|---|---|
| 架构图 | Mermaid flowchart：presentation→domain←data + core/shared 装配 |
| 分层职责 | 表格：5 层职责 / 允许依赖 / 禁止；+ 角色命名约定 |
| 数据流图 | Mermaid sequenceDiagram：登录 UI→Controller→UseCase→Repo→DataSource→ErrorMapper→Result |
| 附加 | bootstrap 启动 flowchart、目录映射、横切设计表、设计动机 |

## 3. 快速上手（Quick Start）

阅读 [docs/ARCHITECTURE.md](../ARCHITECTURE.md)；Mermaid 图在 GitHub / 支持 Mermaid 的 Markdown 预览中渲染。

## 4. 验收命令与实际输出

```
$ # 文档内链接（CONVENTIONS / GETTING_STARTED / adr/0001 / verification 等）
--- missing: 0 ---

$ flutter analyze
No issues found! (ran in 3.6s)
```

数据流图与 `features/auth` 实际链路一致（`LoginController` → `SignInUseCase` → `AuthRepositoryImpl.signIn` → `AuthRemoteDataSource` → `ErrorMapper`，返回 `Result<User>` 经 `fold` 拍平）。

## 5. 与 spec 的偏差与决策

T20.3 决策：选 Mermaid 而非图片，便于随代码演进、diff 友好、无需维护二进制资源。数据流以「登录」真实链路举例（而非抽象示意），与 T19.1 验证报告互相印证。

## 6. 关键文件清单

| 文件 | 说明 |
|---|---|
| `docs/ARCHITECTURE.md` | 架构文档（新建，3 张 Mermaid 图） |

## 7. 下一步任务

T20.4 docs/CONVENTIONS.md（命名 / 代码风格 / Git 提交）。

## 8. 环境快照

Flutter 3.41.9 · Dart 3.11.5 · Clean Architecture + Feature-First · macOS。
