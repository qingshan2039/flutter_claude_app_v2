---
task_id: T20.2
task_name: GETTING_STARTED.md
module_id: M20
module_name: 文档交付
priority: P0
status: completed
verified_at: 2026-05-24
spec_source: flutter_template_v3.md
spec_lines: "802-806"
flutter_version: 3.41.9
dart_version: 3.11.5
deliverables:
  - docs/GETTING_STARTED.md
tags: [docs, getting-started, setup, codegen, multi-env, M20, T20.2]
---

# T20.2 GETTING_STARTED.md — 验证报告

> 任务 T20.2 / 模块 M20 文档交付 / 优先级 P0 / 状态 ✅ completed / 验证 2026-05-24

## TL;DR

T20.2 新建 `docs/GETTING_STARTED.md`：环境准备（版本矩阵 + CocoaPods 固定说明）、依赖安装、代码生成（build_runner + gen-l10n）、环境变量（dart-define + env/*.json）、多环境运行（脚本/原生命令/VSCode/showcase 三方式）、安装验证、打包概览。命令均与仓库实际脚本（`scripts/flutter-env.sh`）对齐。

## 1. 任务目标（来自 spec）

T20.2（spec 802-806）：需含**环境准备、依赖安装、多环境运行命令**，交付物 `docs/GETTING_STARTED.md`。

## 2. 实际配置

| 章节 | 要点 |
|---|---|
| 环境准备 | Flutter 3.41.9/Dart 3.11.5/minSdk24/JDK17/iOS13；CocoaPods 固定原因 |
| 依赖安装 | `flutter pub get` + lefthook install |
| 代码生成 | build_runner build/watch + flutter gen-l10n（区分两套生成机制） |
| 环境变量 | `cp env/dev.example.json env/dev.json` + dev 模板 JSON |
| 多环境运行 | A 脚本 / B 原生命令 / C VSCode / showcase |
| 验证 | analyze + test 双绿 |

## 3. 快速上手（Quick Start）

阅读 [docs/GETTING_STARTED.md](../GETTING_STARTED.md)；与 [README 快速开始](../../README.md) 互补（README 给最短路径，本文给完整解释）。

## 4. 验收命令与实际输出

```
$ # 文档内链接目标存在性校验（含 env/dev.example.json、scripts/*、docs/*）
--- missing: 0 ---

$ flutter analyze
No issues found! (ran in 3.6s)
```

env 模板内容与 `env/dev.example.json` 实际一致（API_BASE_URL / SENTRY_DSN / API_KEY / APP_NAME / ENABLE_LOGGING / ENABLE_CRASH_REPORTING）。

## 5. 与 spec 的偏差与决策

T20.2 决策：明确区分「build_runner 生成」与「flutter gen-l10n 生成」两套机制（新人最易混淆点）；多环境给三种入口（脚本优先），并复述 CocoaPods/SPM 提示无害，减少 issue。

## 6. 关键文件清单

| 文件 | 说明 |
|---|---|
| `docs/GETTING_STARTED.md` | 上手指南（新建） |

## 7. 下一步任务

T20.3 docs/ARCHITECTURE.md（架构图 / 分层职责 / 数据流）。

## 8. 环境快照

Flutter 3.41.9 · Dart 3.11.5 · macOS。
