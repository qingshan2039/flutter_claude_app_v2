---
task_id: T15.4
task_name: VSCode launch.json
module_id: M15
module_name: 多环境配置
priority: P0
status: completed
verified_at: 2026-05-23
spec_source: flutter_template_v3.md
spec_lines: "604-608"
flutter_version: 3.41.9
dart_version: 3.11.5
deliverables:
  - .vscode/launch.json
tags: [env, vscode, launch-json, flavor, dart-define, M15, T15.4]
---

# T15.4 VSCode launch.json — 验证报告

> 任务 T15.4 / 模块 M15 多环境配置 / 优先级 P0 / 状态 ✅ completed / 验证 2026-05-23

## TL;DR

T15.4 新增 `.vscode/launch.json`，含 dev/staging/prod 三套启动配置（各自 `program` = `main_<env>.dart`、`args` = `--flavor <env>` + `--dart-define-from-file=env/<env>.json`），外加 prod 的 profile/release 与 showcase 配置。

## 1. 任务目标（来自 spec）

T15.4 要求 VSCode launch.json（spec 604-608 行）：三套环境启动配置，交付物 `.vscode/launch.json`。

## 2. 实际配置

| 配置名 | program | flavor | mode |
|---|---|---|---|
| dev (debug) | main_dev.dart | dev | debug |
| staging (debug) | main_staging.dart | staging | debug |
| prod (debug) | main_prod.dart | prod | debug |
| prod (profile) | main_prod.dart | prod | profile |
| prod (release) | main_prod.dart | prod | release |
| showcase (dev flavor) | main_showcase.dart | dev | debug |

## 3. 快速上手（Quick Start）

VSCode 左侧 Run and Debug → 顶部下拉选环境 → F5。等价命令见 `scripts/flutter-env.sh`。

## 4. 验收命令与实际输出

```
$ python3 -c "import json;print(len(json.load(open('.vscode/launch.json'))['configurations']),'configs')"
6 configs
```

JSON 合法、6 个配置（含三套环境 + 变体）。

## 5. 与 spec 的偏差与决策

T15.4 决策：除三套必需配置外，补 prod 的 profile/release（性能剖析/发布验证）与 showcase 入口配置，覆盖常用调试场景。每个配置都带 `--dart-define-from-file`，与脚本/CI 注入方式一致。

## 6. 关键文件清单

| 文件 | 说明 |
|---|---|
| `.vscode/launch.json` | 6 个启动配置 |

## 7. 下一步任务

T15.5 资源生成（flutter_launcher_icons / flutter_native_splash / flutter_gen）。

## 8. 环境快照

Flutter 3.41.9 · Dart 3.11.5 · VSCode Dart/Flutter 扩展。
