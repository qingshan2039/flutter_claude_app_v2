---
task_id: T08.6
task_name: 新增语言文档
module_id: M08
module_name: 国际化
priority: P0
status: completed
verified_at: 2026-05-18
spec_source: flutter_template_v3.md
spec_lines: "411-414"
new_doc_files: [docs/EXTEND_GUIDE.md]
section: "新增语言（i18n）"
note: "EXTEND_GUIDE.md 由本任务创建（仅 i18n 章节）；新增 feature/权限/环境章节由 T20.5 补全"
deliverables: ["扩展文档（docs/EXTEND_GUIDE.md i18n 章节）"]
tags: [i18n, documentation, extend-guide, T08, M08, P0]
---

# T08.6 新增语言文档 — 验证报告

> **任务 ID**: T08.6 · **模块**: M08 国际化 · **优先级**: P0 · **状态**: ✅
> **Spec 来源**: [flutter_template_v3.md:411-414](file:///Users/ben/Downloads/flutter_template_v3.md)

## TL;DR

T08.6 创建 [docs/EXTEND_GUIDE.md](file:///Users/ben/ai_project/flutter_claude_app_v2/docs/EXTEND_GUIDE.md)，写「新增语言（i18n）」章节：以新增日语（ja）为例，4 步流程（建 ARB → 登记 kSupportedLocales → gen-l10n → 验证）+ 复数注意事项 + 缺失 key 处理 + 运行时切换用法 + 文案使用方式。EXTEND_GUIDE.md 的其余章节（新增 feature/权限/环境）由 T20.5 补全。

## 1. 任务目标（spec）

- 编写 EXTEND_GUIDE 章节
- **交付物**：扩展文档

## 2. 文档内容结构

「新增语言（i18n）」章节包含：

| 小节 | 内容 |
|---|---|
| 第 1 步 建 ARB | 复制 app_en.arb，改 @@locale，翻译；元数据只在 template 维护 |
| 第 2 步 登记 | `kSupportedLocales` 加 `Locale('ja')` |
| 第 3 步 生成 | `flutter gen-l10n` |
| 第 4 步 验证 | analyze + test + run |
| 复数注意 | 不同语言 CLDR 复数类别不同（en: one/other，zh/ja: other，ar: 6 类） |
| 缺失 key | gen-l10n 默认回退 template 语言；`untranslated-messages-file` 检查 |
| 运行时切换 | `setLocale` / `useSystemLocale`（已内置） |
| 文案使用 | `AppLocalizations.of(context).xxx` 各形态示例 |

## 3. 与 T20.5 的关系

spec 中 EXTEND_GUIDE.md 是 T20.5 的完整交付物（含新增 feature/语言/权限/环境 4 章）。T08.6 只负责其中「新增语言」章节。文档 frontmatter 标注 `status: in_progress`，目录列出待补章节并标 `(T20.5)`。

## 4. 5 项验证

| # | 验证 | 结果 |
|---|---|---|
| 1 | 依赖注册 | 不适用（文档） |
| 2 | 代码生成 | 不适用 |
| 3 | 运行调用 | 不适用 |
| 4 | 单元测试 | 不适用（文档任务） |
| 5 | 静态分析 | ✅ 文档未引入 Dart 代码，全工程 analyze 仍 No issues |

文档质量自检：4 步流程可操作、代码片段准确（与 [locale_provider.dart] 一致）、含复数陷阱与缺失 key 处理等实战要点。

## 5. 关键文件

| 路径 | 说明 |
|---|---|
| `docs/EXTEND_GUIDE.md` | 扩展指南，含「新增语言」完整章节 |

## 6. 下一步

T08.6 完成后，**M08 国际化全部完成**（T08.1-T08.6 共 6 个子任务）。下一步进入 **M09 权限管理**：

- T09.1 封装 PermissionService
- T09.2 覆盖常用权限（相机/相册/麦克风/定位/通知/存储/蓝牙）
- T09.3 永久拒绝处理
- T09.4 iOS / Android 差异封装

---

**验证人**: Claude (Opus 4.7) · **日期**: 2026-05-18
