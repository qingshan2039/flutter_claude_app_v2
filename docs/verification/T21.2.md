---
task_id: T21.2
task_name: 渲染优化规范
module_id: M21
module_name: 性能优化体系
priority: P1
status: completed
verified_at: 2026-05-24
spec_source: flutter_template_v3.md
spec_lines: "847-851"
flutter_version: 3.41.9
dart_version: 3.11.5
deliverables:
  - docs/PERFORMANCE.md
tags: [performance, const, repaint-boundary, riverpod-select, rendering, M21, T21.2]
---

# T21.2 渲染优化规范 — 验证报告

> 任务 T21.2 / 模块 M21 性能优化体系 / 优先级 P1 / 状态 ✅ completed / 验证 2026-05-24

## TL;DR

T21.2 新建 `docs/PERFORMANCE.md`：渲染优化三件套——**const 规范**（复用、跳过重建）、**RepaintBoundary 使用指南**（何时用/不滥用 + 列表示例）、**Riverpod `select()` 精准订阅**（替代 Selector，收窄 rebuild 半径）；并整合列表/图片/包体积/启动埋点章节与性能预算，成为 M21 的总入口文档。

## 1. 任务目标（来自 spec）

T21.2（spec 847-851）：const 规范 + RepaintBoundary 使用指南 + Selector 精准订阅示例，交付物 `docs/PERFORMANCE.md`。

## 2. 实际配置

| spec 要求 | 实现 |
|---|---|
| const 规范 | 能 const 一律 const；提取静态子树为 const；引用 very_good 的 prefer_const lint |
| RepaintBoundary | 该用场景（长列表行/动画旁/CustomPaint）+ 不滥用 + DevTools Highlight Repaints 验证 |
| Selector 精准订阅 | Riverpod `provider.select((s) => s.field)`；下沉 watch 到最小 Widget；`ref.listen` 做副作用 |
| 附加 | 列表(§2)/图片(§3)/包体积(§4)/启动埋点 章节 + 性能预算表 |

## 3. 快速上手（Quick Start）

阅读 [docs/PERFORMANCE.md](../PERFORMANCE.md)；与 [DEVTOOLS_GUIDE](../DEVTOOLS_GUIDE.md) 配合（规范说怎么写，DevTools 说怎么测）。

## 4. 验收命令与实际输出

```
$ # 文档链接（high_performance_list_page / app_image / 脚本 / DEVTOOLS_GUIDE）存在性
--- 全部命中 ---

$ flutter analyze
No issues found! (ran in 2.6s)
```

文档中的代码示例与实际实现一致：RepaintBoundary 用法对应 `HighPerformanceListPage`（T21.3）；图片解码对应 `AppImage.thumbnail`（T21.4）。

## 5. 与 spec 的偏差与决策

T21.2 决策：把 PERFORMANCE.md 做成 M21 的总览文档（不止渲染），把列表/图片/包体积/启动埋点串成一篇，避免知识点散落；Selector 用 Riverpod 原生 `select()` 表达（本项目状态管理是 Riverpod，见 [ADR-0001](../adr/0001-handwritten-providers-over-codegen.md)）。

## 6. 关键文件清单

| 文件 | 说明 |
|---|---|
| `docs/PERFORMANCE.md` | 性能优化规范（新建，渲染/列表/图片/包体积/启动/预算） |

## 7. 下一步任务

T21.3 列表性能优化（高性能长列表示例）。

## 8. 环境快照

Flutter 3.41.9 · Dart 3.11.5 · very_good_analysis（const lints）· macOS。
