---
task_id: T20.5
task_name: EXTEND_GUIDE.md
module_id: M20
module_name: 文档交付
priority: P0
status: completed
verified_at: 2026-05-24
spec_source: flutter_template_v3.md
spec_lines: "820-825"
flutter_version: 3.41.9
dart_version: 3.11.5
deliverables:
  - docs/EXTEND_GUIDE.md
tags: [docs, extend-guide, feature, i18n, permission, environment, M20, T20.5]
---

# T20.5 EXTEND_GUIDE.md — 验证报告

> 任务 T20.5 / 模块 M20 文档交付 / 优先级 P0 / 状态 ✅ completed / 验证 2026-05-24

## TL;DR

T20.5 扩展既有 `docs/EXTEND_GUIDE.md`（原仅含 T08.6 的「新增语言」章节）：补全**新增 feature（6 步分层骨架 + 可照抄代码）**、**新增权限（4 步：枚举/原生声明/UI/调用）**、**新增环境（7 步：枚举/EnvConfig/入口/env 文件/原生 flavor/脚本+VSCode/验证）**，并更新 frontmatter（status→completed）、目录与导言。

## 1. 任务目标（来自 spec）

T20.5（spec 820-825）：需含**新增 feature / 新增语言 / 新增权限 / 新增环境**步骤，交付物 `docs/EXTEND_GUIDE.md`。

## 2. 实际配置

| 章节 | 来源 | 要点 |
|---|---|---|
| 新增 feature | T20.5 新增 | 6 步：目录骨架 → domain → data → presentation → 路由 → 生成验证 |
| 新增语言 | T08.6 已有 | 4 步 ARB → 登记 → gen-l10n → 验证 |
| 新增权限 | T20.5 新增 | 4 步：扩 AppPermission 枚举 → 原生声明（Manifest/Info.plist）→ UI 文案 → 调用 |
| 新增环境 | T20.5 新增 | 7 步：AppEnvironment → EnvConfig → main_x.dart → env json → flavor → 脚本/VSCode → 验证 |

## 3. 快速上手（Quick Start）

阅读 [docs/EXTEND_GUIDE.md](../EXTEND_GUIDE.md)；各节均给出可复制的代码/命令，以 `profile` / `calendar` / `qa` 为具体示例。

## 4. 验收命令与实际输出

```
$ # 四个章节锚点齐全
$ grep -cE "^## 新增" docs/EXTEND_GUIDE.md
4

$ flutter analyze
No issues found! (ran in 3.6s)
```

新增章节内容与实际代码对齐：feature 步骤参考 `features/auth|detail|home`；权限枚举与 `lib/core/permission/app_permission.dart`（camera…bluetooth）一致；环境步骤与 `AppEnvironment`（dev/staging/prod）、`scripts/flutter-env.sh` 白名单一致。

## 5. 与 spec 的偏差与决策

T20.5 决策：保留 T08.6 既有「新增语言」章节原文（仅调整在目录中的顺序），其余三章新增；每章用具体示例名（profile/calendar/qa）而非占位符，降低照抄成本。强调「Dart 入口 + 原生 flavor 必须成对」这一最易踩的坑。

## 6. 关键文件清单

| 文件 | 说明 |
|---|---|
| `docs/EXTEND_GUIDE.md` | 扩展指南（扩展：+feature/+权限/+环境三章，更新 frontmatter/目录） |

## 7. 下一步任务

T20.6 docs/TROUBLESHOOTING.md（build_runner / iOS Pod / 常见报错）。

## 8. 环境快照

Flutter 3.41.9 · Dart 3.11.5 · macOS。
