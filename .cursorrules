# Cursor Rules — flutter_claude_app_v2

可商用 Flutter 模板。Flutter 3.41.9 / Dart 3.11.5 / Material 3 / Clean Architecture + Feature-First。
完整规则见 CLAUDE.md 与 docs/CONVENTIONS.md；以下为生成代码时必须遵守的要点。

## 架构
- 分层 domain → data → presentation，依赖只能由外向内。
- lib/core/ 放跨模块基础设施；lib/features/<f>/ 放业务，feature 之间禁止互相 import。
- 命名：文件 lower_snake_case，类 UpperCamelCase，顶层常量 kXxx，私有 _xxx。
  角色后缀 XxxRepository(abstract)/XxxRepositoryImpl/XxxModel/XxxUseCase/xxxProvider/XxxPage。

## 技术选型（不要替换）
- DI：get_it + injectable（codegen）。状态：riverpod 2.6（手写 provider）。路由：go_router 15。
- 模型：freezed + json_serializable（DTO）；领域实体不可变纯 Dart。网络：dio + retrofit。
- 测试：flutter_test + mocktail。Lint：very_good_analysis 10.2.0。
- 零新增依赖优先：外部能力用抽象 + MethodChannel seam + 桩实现降级，除非 spec 点名某 package。

## injectable / codegen 三个坑
1. 构造函数别用基本类型参数（int/String/bool）做注入——无法解析；改用带默认值的 public 可变字段。
2. @disposeMethod 以接口绑定时生成 i.dispose()，i 是接口类型——接口必须声明该方法，否则生成代码编译失败。
3. flutter analyze 不查生成代码（*.config.dart 被 exclude）；改了 DI/模型后务必：
   build_runner → analyze → flutter test（test 才会编译生成代码、暴露错误）。

## 编码
- const 优先。import 用 package: 绝对路径。错误处理跨层返回 Result<T>，不抛异常，data 层经 ErrorMapper 归一。
- 不要盲目 `dart fix --apply`：avoid_redundant_argument_values 与 unnecessary_lambdas 的自动修复曾改坏代码。
- 改完代码必须 flutter analyze + flutter test 双绿。

## 测试
- 复用 test/_helpers/（InMemoryKeyValueStorage / RecordingAnalytics / FakeRemoteConfig）。
- Widget 测试用 ProviderScope；涉及 DI 时 getIt.reset() + configureDependencies(environment:'dev')。
- 异步/流 UI 断言前 await tester.pumpAndSettle()。

## 交付约定
- 每完成一个 spec 任务：写 docs/verification/T{XX.X}.md（8 段模板）+ 更新 README 索引 + 视情况加 showcase demo 页。
- 提交走 Conventional Commits；未经明确指示不要 git commit；绝不提交密钥。
