name: flutter_claude_app_v2
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Dependency injection (M02 / T02.1)
  get_it: ^8.0.0
  injectable: ^2.5.0

  # Data modeling (M02 / T02.2)
  freezed_annotation: ^3.0.0
  json_annotation: ^4.12.0

  # Network (M04 / T04.x)
  dio: ^5.7.0
  retrofit: ^4.4.0

  # Storage (M05 / T05.x)
  shared_preferences: ^2.3.0
  flutter_secure_storage: ^9.2.0
  hive_ce: ^2.10.0
  hive_ce_flutter: ^2.0.0
  path_provider: ^2.1.4

  # State management (M06 / T06.x)
  flutter_riverpod: ^2.6.0

  # Routing (M07 / T07.x)
  go_router: ^15.0.0

  # Internationalization (M08 / T08.x)
  flutter_localizations:
    sdk: flutter
  intl: any

  # Permissions (M09 / T09.x)
  permission_handler: ^11.3.0

  # Logging & monitoring (M11 / T11.x)
  logger: ^2.4.0
  sentry_flutter: ^9.0.0
  cached_network_image: ^3.4.1
  easy_refresh: ^3.5.0
  meta: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # Code generation (M02 / T02.1 + T02.2, M04 / T04.6, M06 / T06.x)
  build_runner: ^2.4.13
  injectable_generator: ^2.6.2
  freezed: ^3.0.0
  json_serializable: ^6.8.0
  retrofit_generator: ^10.0.0
  # riverpod_generator 与当前 json_serializable / freezed / retrofit 版本链不兼容；
  # 改用手写 Provider 模式（与 codegen 等价），完整说明见 docs/verification/T06.1.md
  go_router_builder: ^4.3.0

  # Native splash (M13 / T13.4)
  flutter_native_splash: ^2.4.0
  flutter_launcher_icons: ^0.14.4
  flutter_gen_runner: ^5.14.1
  very_good_analysis: ^10.2.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # M08/T08.1: 启用 flutter gen-l10n 在 lib/l10n/ 下生成 AppLocalizations
  generate: true

  # 项目级固定使用 CocoaPods（关闭 Swift Package Manager）。
  # 原因：permission_handler_apple（M09）与 flutter_secure_storage（M05）目前仅提供
  # podspec、未适配 SPM。固定 CocoaPods 可避免在开启 SPM 的机器上发生意外的 SPM 迁移。
  #
  # 注意：iOS 相关命令时 flutter_tools 仍会无条件打印一行
  #   "The following plugins do not support Swift Package Manager for ios: ..."
  # 这是 macOS 上检测到「仅 CocoaPods」插件时的信息性提示（见 flutter_tools
  # darwin_dependency_management.dart，仅受 host==macOS 限制，与本开关无关），
  # 无法通过配置消除，且无害——两个插件有 podspec，iOS 始终用 CocoaPods 正常构建。
  # 待两个插件适配 SPM 后删除本段即可切回 SPM。
  config:
    enable-swift-package-manager: false

  # M15/T15.5: 资源目录（flutter_gen 据此生成 lib/gen/assets.gen.dart 类型安全引用）
  assets:
    - assets/images/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# M15/T15.5: flutter_gen 配置（顶层键）。
# `dart run build_runner build` 会据 flutter.assets 生成 lib/gen/assets.gen.dart，
# 用 Assets.images.placeholder 取代裸字符串路径（编译期校验、可重构）。
# 如需 SVG 渲染集成，添加 flutter_svg 依赖后在 integrations 下打开 flutter_svg: true。
flutter_gen:
  output: lib/gen/
  line_length: 80
